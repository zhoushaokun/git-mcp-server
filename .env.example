# Example Environment Variables for git-mcp-server
# Copy this file to .env and modify the values as needed.

# --- MCP Server Configuration ---

# Transport mechanism: 'stdio' (default) or 'http'
# MCP_TRANSPORT_TYPE=stdio

# --- HTTP Transport Settings (only used if MCP_TRANSPORT_TYPE=http) ---

# Port for the HTTP server
# MCP_HTTP_PORT=3000

# Host address for the HTTP server to bind to
# MCP_HTTP_HOST=127.0.0.1

# Comma-separated list of allowed origins for CORS (e.g., http://localhost:8080,https://my-app.com)
# MCP_ALLOWED_ORIGINS=

# --- Server Identification ---

# Optional: Override the server name reported during MCP initialization
# MCP_SERVER_NAME=git-mcp-server

# --- Logging Configuration ---

# Logging level: 'debug', 'info', 'notice', 'warning', 'error', 'crit', 'alert', 'emerg'
# LOG_LEVEL=info

# Optional: Path to the log file. If commented out or empty, logs only to console.
# LOG_FILE_PATH=./logs/server.log

# Optional: Maximum size of a single log file before rotation (in MB)
# LOG_MAX_FILE_SIZE_MB=10

# Optional: Maximum number of rotated log files to keep
# LOG_MAX_FILES=5

# Optional: Whether to compress rotated log files ('true' or 'false')
# LOG_ZIP_ARCHIVES=true

# Optional: Comma-separated list of additional regex patterns for redacting sensitive data in logs.
# LOG_REDACT_PATTERNS=
